# ==============================================================================
# Configuración para Desarrollo Local
# ==============================================================================
# Este archivo contiene valores por defecto para desarrollo local
# Copiar a .env para usar: cp .env.local .env

# ==============================================================================
# Base de Datos PostgreSQL (Kong) - Contenedor Local
# ==============================================================================
POSTGRES_HOST=kong-db
POSTGRES_PORT=5432
POSTGRES_DB=kong
POSTGRES_USER=kong
POSTGRES_PASSWORD=kong_local_password
POSTGRES_SSL=off

# Variables Kong (mapean a las POSTGRES_* anteriores)
KONG_DATABASE=postgres
KONG_PG_HOST=${POSTGRES_HOST}
KONG_PG_PORT=${POSTGRES_PORT}
KONG_PG_DATABASE=${POSTGRES_DB}
KONG_PG_USER=${POSTGRES_USER}
KONG_PG_PASSWORD=${POSTGRES_PASSWORD}
KONG_PG_SSL=${POSTGRES_SSL}
KONG_PG_SSL_VERIFY=off

# ==============================================================================
# Base de Datos MySQL (Konga) - Contenedor Local
# ==============================================================================
DB_ADAPTER=mysql
DB_HOST=konga-db
DB_PORT=3306
DB_DATABASE=konga
DB_USER=konga
DB_PASSWORD=konga_local_password

# Variables Konga DB (mapean a las DB_* anteriores)
KONGA_PG_DATABASE=${DB_DATABASE}
KONGA_PG_USER=${DB_USER}
KONGA_PG_PASSWORD=${DB_PASSWORD}

# ==============================================================================
# Puertos Kong (valores por defecto)
# ==============================================================================
KONG_ADMIN_PORT=8001
KONG_PROXY_PORT=8000
KONG_PROXY_SSL_PORT=8443
KONG_ADMIN_SSL_PORT=8444

# ==============================================================================
# Passwords de Root (solo para local)
# ==============================================================================
KONGA_RP_PASSWORD=konga_root_local

# ==============================================================================
# Identificador de Ambiente
# ==============================================================================
ENVIRONMENT=local

# ==============================================================================
# NOTAS PARA DESARROLLO LOCAL:
# - Backend URLs: Configuradas en config/kong/kong-dev.yaml (httpbin.org por defecto)
# - Keycloak: URLs en config/kong/kong-nonprod.yaml
# - Para cambiar URLs: editar en Konga UI (http://localhost:3366/konga)
# - Logs: Todos los servicios escriben a stdout/stderr para debugging
# ==============================================================================
